<script>
  document.querySelectorAll(".astro-code .line").forEach((line, i) => {
    const num = document.createElement("span");
    num.className = "line-number";
    num.textContent = String(i + 1);
    line.prepend(num);
  });
</script>

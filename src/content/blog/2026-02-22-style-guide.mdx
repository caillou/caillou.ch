---
title: "style guide"
date: 2026-02-22
description: "A reference of all available markdown formatting options."
tags: ["meta", "style-guide", "markdown"]
---

import { Picture } from "astro:assets";
import portrait from "../../assets/portrait.jpg";
import portraitStanding from "../../assets/portrait-standing.jpg";

This is a style guide for all the markdown formatting options available on this site. I use this when I want to check the styles during development.

This is a paragraph with **bold text**, _italic text_, `inline code`, and a [link to nowhere](#). It serves as a quick reference for all the markdown options available on this site.

## Heading 2

Subheadings are created with hash symbols. The number of hashes indicates the heading level. For example, `##` creates a level 2 heading, while `###` creates a level 3 heading.

### Heading 3

This is a paragraph. Obviously, we can have multiple paragraphs. I add some filler test here to show the spacing between consecutive text blocks.

Paragraphs are separated by blank lines. This is a second paragraph to show the spacing between consecutive text blocks.

### Unordered list

- First item
- Second item
- Third item with **bold** and _italic_

### Ordered list

1. First item
2. Second item
3. Third item with `inline code`

### Blockquote

> This is a blockquote. It can contain **bold**, _italic_, and `inline code`. It gets a green left border and a dark background.

### Code block

```typescript
interface Post {
  title: string;
  date: Date;
  description?: string;
  tags: string[];
}

function greet(name: string): string {
  // this is a comment
  return `Hello, ${name}!`;
}
```

---

### Picture

<figure class="max-lg:full-bleed">
  <Picture
    src={portrait}
    alt="Example image"
    widths={[800, 1600, 2400, 3200]}
    formats={["avif", "webp"]}
    quality={80}
  />
  <figcaption>
    FIG 1.0: A regular picture, full-bleed on mobile only.
  </figcaption>
</figure>

### Full-bleed picture

<figure class="full-bleed">
  <Picture
    src={portraitStanding}
    alt="Example full-bleed image"
    widths={[800, 1600, 2400, 3200]}
    formats={["avif", "webp"]}
    quality={80}
  />
  <figcaption>
    FIG 1.1: A full-bleed picture, spanning the entire viewport width.
  </figcaption>
</figure>

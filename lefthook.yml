pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,ts,mjs,astro,css,md,json,yaml,yml}"
      staged: true
      run: pnpm prettier --write {staged_files}
      stage_fixed: true
    lint:
      glob: "*.{js,ts,mjs,astro,css,json}"
      staged: true
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    astro-check:
      run: pnpm astro check
